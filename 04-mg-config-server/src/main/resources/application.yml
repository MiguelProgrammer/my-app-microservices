server:
  port: 8888
spring:
  application:
    name: mg-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/MiguelProgrammer/mg-config-server
          default-label: main
          search-paths:
          - 'greeting-service*'
